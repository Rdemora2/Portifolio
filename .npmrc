# Dependency lifecycle scripts are disabled as a supply-chain boundary.
# Project scripts explicitly invoked through `npm run` remain available.
ignore-scripts=true

# Refuse installs when the local runtime falls outside package.json#engines.
engine-strict=true
